# Raw Page Captures

Store saved HTML, markdown exports, or metadata snapshots of Neuromatch course pages here when reproducible parsing matters.

Raw captures should be treated as source inputs, not notes. Convert insights into `notes/`, `learning-log/`, `papers/`, or `experiments/`.
