# Datasets

Store dataset metadata, source links, checksums, and small helper notes here.

Do not commit large raw datasets by default. For restricted datasets, keep only:

- source URL;
- access notes;
- license or redistribution warning;
- expected local path if used by an experiment;
- minimal verification command if there is one.

Large data should stay outside Git unless the repository intentionally adds a data-management policy.
